    \section{Zentralisiertes Datenmanagement und Merkmalsextraktion für den Threat Report}

    Um die Interoperabilität und Standardisierung des Threat Reports zu gewährleisten, wurde entschieden, die erforderlichen Daten zentral aus der Lokalen Prometheus-Datenbank zu extrahieren, anstatt diese einzeln von den jeweiligen Detection-Einheiten abzurufen. Hierfür wurden spezifische Prometheus-Exporter für die Detektionseinheiten entworfen und integriert. Diese stellen die Daten kontinuierlich über die lokalen Ports 2112, 2113, 2115 und 2116 bereit.

    Die über die Exporter gelieferten Datensätze umfassen sowohl die Klassifizierung der Anomalien (Attack oder Benign) als auch sämtliche Features, die für die jeweilige Detektion herangezogen wurden. Dieser umfassende Ansatz ist notwendig, da eine zukünftige Optimierung der Gesamtdetektion mittels eines Machine-Learning-Modells vorgesehen ist.\\
    Dieses Modell soll zunächst einen Feature-Selection-Prozess durchlaufen, um die signifikantesten Merkmale für die Detektion zu identifizieren, welches ebenfalls im Kapitel~\ref{chapter:diskussion_und_forschungsluecke} (Diskussion und Forschungslücke) näher beschrieben ist. \todohl{und somit die Vorhersagegüte zu maximieren}{paper reference ML besser mit vorselection} \cite{heigl2021exploiting}(Bessere erkennung mit mehr Input Daten) \cite{kotenko2022systematic} (strukturierten Datenkorrelation für verbessere IDS-Systeme). Sollten zum jetzigen Zeitpunkt nicht alle verfügbaren Features bereitgestellt werden, besteht das Risiko, dass informationstechnisch relevante Datenpunkte für die spätere Modellbildung verloren gehen \todohl{und die Detektionsrate gemindert wird}{Paper reference ML vorselection besser mit möglichst großen input über alle datenpunkte}. Die Grundlage für diesen Abschnitt bildete eine detaillierte Analyse der Detektionseinheiten sowie ein tiefgreifendes Verständnis der bereitgestellten Datenstrukturen, wie bereits in Kapitel~\ref{sec:detection_einheiten_der_ladeinfrastruktur} (Detection Einheiten der Ladeinfrastruktur) dargelegt.

    Der Threat Report importiert diese Daten über Prometheus und überführt sie in ein CSV-Format. \todohl{Das CSV-Format wurde aufgrund seiner weitreichenden Kompatibilität und einfachen Handhabung gewählt. In Bezug auf die Performance ist CSV für das vorliegende Szenario ausreichend, da bei einer moderaten Datenmenge von maximal 200 Zeilen pro Report keine signifikanten Vorteile durch komplexere Formate wie .parquet zu erwarten sind.}{paper reference needed} \cite{Becker2009} (Theoretische Basis für leichtgewichtiges Format bei gerinen Datenmengen)

    \section{Zeitliche Diskretisierung und Intervalldefinition}

    Um die Systemressourcen des Raspberry Pi Zero 2 W zu schonen und gleichzeitig das Datenaufkommen zu begrenzen, ohne die \todohl{Detektionssicherheit zu beeinträchtigen, wurde ein Zeitfenster von 60 Sekunden gewählt, das in 30-Sekunden-Intervallen unterteilt ist. Dieser Ansatz ermöglicht es, umfassende Informationen der Detektionseinheiten zu erhalten und basiert auf etablierten wissenschaftlichen Methoden}{paper reference needed}. 

    Die Parameter für diese Zeitintervalle wurden bewusst variabel gestaltet. Dies schafft die Grundlage für zukünftige Untersuchungen, wie sie in Kapitel~\ref{chapter:diskussion_und_forschungsluecke} (Diskussion und Forschungslücke) beschrieben sind, um den Einfluss unterschiedlicher Zeitfenster auf die Qualität der Reports empirisch zu evaluieren.\\
    Die aktuell implementierte Sequenz umfasst drei Datenpunkte: den aktuellen Zeitpunkt (0 Sekunden) sowie die Zustände der Detektionseinheiten vor 30 und 60 Sekunden. Durch diese Struktur wird eine Timeline generiert, die den zeitlichen Verlauf der Detektion über drei Messpunkte abbildet.

    \section{Statistische Merkmalsaggregation mittels Sliding-Window-Analyse}

    Neben den instantanen Merkmalswerten (Features) und Anomalie-Indikatoren der Detektionseinheit werden zusätzlich historische Daten mittels \textit{Sliding Windows} importiert. Pro Threat Report werden drei dieser Fenster definiert, welche jeweils den aktuellen Datenpunkt in Relation zu zwei vorangegangenen Zeitpunkten setzen:

    \begin{itemize}
        \item \textbf{Window 1:} 0 Sekunden im Vergleich mit -30 s und -60 s
        \item \textbf{Window 2:} -30 Sekunden im Vergleich mit -60 s und -90 s
        \item \textbf{Window 3:} -60 Sekunden im Vergleich mit -120 s und -150 s
    \end{itemize}

    Innerhalb dieser Zeitfenster werden für die statistische Auswertung folgende Kennzahlen berechnet:
    \begin{itemize}
        \item \textbf{Mittelwert (Average):} Der Durchschnittswert innerhalb des Fensters.
        \item \textbf{Extremwerte:} Minimum und Maximum im jeweiligen Intervall.
        \item \textbf{Median:} Der Zentralwert der Datenpunkte.
        \item \textbf{Zuwachs (Increase):} Die relative oder absolute Veränderung innerhalb des Fensters.
        \item \textbf{Summe:} Die kumulierten Werte des jeweiligen Zeitraums.
    \end{itemize}

    Durch diese Methodik entsteht ein umfassender Report, der nicht nur eine Momentaufnahme der Detektionseinheiten liefert, sondern auch die zeitliche Entwicklung über die letzten 60 Sekunden bzw. durch die Sliding Windows bis zu 150 Sekunden in die Vergangenheit abbildet. Diese Datenbasis dient als Input für eine spätere Aufbereitung und trainings eines Machine-Learning-Modells, welcher in Kapitel~\ref{chapter:diskussion_und_forschungsluecke} (Diskussion und Forschungslücke) beschrieben sind.\\
    \todohl{Der für diesen Abschnitt implementierte Quellcode, welcher die Logik der Datenabfrage sowie die Generierung des Reports umfasst, ist im Anhang unter \texttt{reporter.py} und \texttt{queries.py} dokumentiert.}{reference noch einfügen Latex style}